# Backend (Express + MySQL)
# Copia este archivo como .env y ajusta lo necesario.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=payment_wallet_db
PORT=3000
FRONTEND_ORIGINS=http://localhost:5174

# JWT
JWT_SECRET=cambia-esto-por-un-secreto
JWT_EXPIRES=4h
